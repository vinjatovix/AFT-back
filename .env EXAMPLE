NODE_ENV = "development"
PORT = 5000
UPLOAD_DIRECTORY = "/tmp"

DB_HOST=localhost
DB_USER =aft
DB_PASSWORD=localPassword // to run in local docker
DB_DATABASE=aft // to run in local docker
DB_REPLICA_SET=rs0 // to run in local docker

